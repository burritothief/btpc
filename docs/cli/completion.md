# Shell completion and manual pages

Generate or install completions without editing shell startup files:

```console
btpc completion generate bash
btpc completion install zsh --dry-run
btpc completion uninstall fish
```

BTPC supports Bash, Zsh, Fish, PowerShell, and Elvish user completion directories.
Generate the manual page with `btpc manpage` and redirect it into a local manpath.

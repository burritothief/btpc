# BitTorrent v2

BEP 52 represents files in a nested file tree and hashes each file with a SHA-256
Merkle tree. Large files may require top-level piece layers. Paths and dictionary
keys are raw byte strings, not guaranteed UTF-8 text.

The v2 exact-topic value uses the SHA-256 info hash encoded as a multihash in magnet
URIs.
